name: "Sample React CiCd Pipeline"

on:
    push:
        branches:
            - main
            - master

jobs:
    build_and_test:
        runs-on: ubuntu_latest

        steps:
            - name: "Setup Node js"
              uses: actions/setup-node@v4
              with:
                node-version: '21'
